const DB = 'db';

const routes = {
  AUTH: '/auth',
  PROFILE: '/api/profile',
  VOCAB: '/api/vocab',
};

const urlParams = {
  userId: ':userId',
};

module.exports = {
  DB,
  routes,
  urlParams,
};
