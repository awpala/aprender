import React, { useState } from 'react';

export const UserContext = React.createContext();

export const Provider = (props) => {
    // TO-DO: state variables

    // TO-DO: event handlers & actions

    // TO-DO: return component
}